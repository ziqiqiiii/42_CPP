#include "../headers/whatever.hpp"

int main()
{
    int a = 2;
    int b = 3;
    cout << "a = " << a << ", b = " << b << endl;
    cout << "max(a, b) = " << max(a, b) << endl;
    cout << "min(a, b) = " << min(a, b) << endl;

    return 0;
}
